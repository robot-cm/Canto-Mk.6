# Canto Mk.6 底层改造设计文档

> 阶段 0 调研产出 —— 基于源码实测，非 AGENTS.md 的设想。
> 目标硬件：Seeed XIAO ESP32-S3（ESP32-S3R8，8MB Flash / 8MB PSRAM）+ 1.28" 圆屏（GC9A01 + CST816 + microSD + 板载 RTC）。

---

## 1. 现状调研结论（已逐一核实）

### 1.1 插件运行时：已成熟，不是空白
AGENTS.md 把「插件语言/插件系统」列为待定，但代码里**已经实现**了一套完整方案：

- **JerryScript 已集成**（`third_party/jerryscript`），编译开关在 `CMakeLists.txt`（VM halt / line info / error message 已开）。
- **三层结构**：
  - `src/script_engine/core/script_engine_core.c` —— VM 生命周期、realm 创建/切换、JS 解析执行、超时检测、manifest 读取（`script_engine_get_manifest`）、`main.js` 解析（`:1282`）。
  - `src/script_engine/spm/spm.c` —— Script Program Manager，独占脚本程序生命周期。用双向链表 `program_list` 管理 `script_program_t`，状态机：`TERMINATED / STOPPING / ACTIVE / SUSPENDED`；每个程序持 `sni_ctx + script_pkg + realm + cleanup_view`。
  - `src/script_engine/sni/` —— Script Native Interface，JS↔原生桥。**这就是 Plugin API 的承载层**：`sni_api_lv`（暴露 LVGL 控件）与 `sni_api_eos`（暴露系统能力），权限在 `sni_api_eos_permission.c` 校验（manifest 声明 `permissions`）。
- **插件包格式 = `.eapk`（app）/ `.ewpk`（watchface）**：含 `manifest.json` + `main.js` + `icon.bin`。
  - 安装：`eos_app_install(eapk_path)` → `eos_pkg_read_header` → `eos_pkg_mgr_unpack` → 解包到安装目录。
  - 路径（`src/services/storage/eos_storage_paths.h`）：`EOS_SYS_DIR` + `app/apps/`（已装）、`app/app_data/`（数据）。`EOS_SYS_DIR` 在 MCU 上为项目 `fs/`，Native 为 `build/fs/`，WASM 为 `/`。
  - 列表/排序/卸载：`eos_app_list_*`、`eos_app_install/uninstall`、`eos_app_order_move`（见 `src/framework/app/eos_app.h`）。
- **结论**：AGENTS.md 设想的「JS 插件 + Plugin API」**框架已经存在**。底层大改的重点不是重造插件系统，而是**把缺失的真机板级移植和真实驱动接上，并让 PSRAM/圆屏统一**。

### 1.2 设备 HAL：注册式，但驱动缺失
- 位置：`src/devices/{display,time,battery,power,sensor,speaker,microphone,vibrator}`。
- 模式（以 `eos_dev_display.c` 为例）：`xxx_register(ops)` 只保存 `ops` 指针与 `state`，`get_instance()` 返回单例。**纯管理器，不含任何硬件代码。**
- **全仓库没有任何地方调用 `eos_dev_*_register`**（仅定义）。说明「板级层」缺失 —— GC9A01 / CST816 / SD / RTC 的实际驱动是空壳。
- **缺口**：`devices/` 里**没有 touch、storage 的 HAL**（触摸目前并入显示端口的 `lv_indev`；存储走 `services/storage/eos_lvgl_fs.c` 这个 service 层，用 LVGL 文件系统接口，见 1.3）。RTC 有 `eos_dev_time` 桩模式但无 PCF8563 实现。

### 1.3 显示 / 输入接管
- **模拟器**：`src/lib/utils/eos_virtual_display.c`（`#if EOS_USE_VIRTUAL_DISPLAY`）。内部 `lv_display_create` + canvas + `_virtual_display_flush_cb`（把渲染结果 memcpy 到 canvas），SDL2 事件经 `_canvas_event_cb` → `lv_indev`（`_virtual_input_read`）。这是当前唯一可用的显示后端。
- **真机需要**：GC9A01 的 `lv_display` 端口（SPI `flush_cb` + PSRAM 帧缓冲）+ CST816 的 `lv_indev` 端口（I2C `read_cb`）。

### 1.4 构建系统：目前只有模拟器
- 根 `CMakeLists.txt`：把 OS 编成静态库 `elenixos_lib`（`file(GLOB_RECURSE src/**/*.c)`），链接 cJSON / JerryScript / LVGL。通过 `EOS_PLATFORM`（Native / WASM）选择 `EOS_SYS_ROOT_DIR`。
- `simulator/CMakeLists.txt` 编出 `elenixos_sim.exe`。
- **整个仓库没有 ESP-IDF 的 `main` 组件、`sdkconfig.defaults`、`partitions.csv`**；`src` 内也无 FreeRTOS 任务创建（模拟器是 SDL2 单线程主循环）。
- **结论**：要让 Canto Mk.6 跑在 XIAO 上，必须**新建 ESP-IDF 板级移植**。这是「底层大改」的主体工作量。

### 1.5 网络服务：确认缺失
全仓 `src` 无真正的 Wi-Fi / SOCKS5 / TCP 连接实现（仅 `eos_test.c` 字符串命中与图标命名）。符合 AGENTS.md 的判断。

---

## 2. 「底层大改」真实范围（修正 AGENTS.md）

把重点从「重造插件系统」改为「补齐缺失的板级移植 + 真实驱动 + 统一 PSRAM/圆屏」，插件系统只做**收口与真机验证**。

| AGENTS.md 原计划 | 实测后修正 |
|---|---|
| 评估插件语言（JS/VM/原生） | **已定 JerryScript**，跳过评估 |
| 自写 Plugin Manager | **SPM + eos_app 已实现**，改为真机装载验证 |
| 自写 Plugin API | **SNI(eos + lv) 已实现**，改为补全/收口 |
| 硬件驱动全空 | 确认空壳，**新写 GC9A01/CST816/SD/RTC** |
| 无构建目标 | **新建 ESP-IDF 板级移植**（最大块） |
| Shell | 仍待建（src 无） |

---

## 3. 分阶段改造方案（文件/函数级）

### 阶段 A · ESP-IDF 板级移植（新建，最大块）
- 新建 `main/` 组件：`CMakeLists.txt`（`idf_component_register(SRCS "main.c" ...)`），`main.c` 负责：
  1. `nvs_flash_init()`；
  2. 创建 FreeRTOS 任务：
     - `ui_task`：LVGL `lv_timer_handler()` 周期调用 + 渲染驱动；
     - `input_task`：轮询 CST816 → 喂 `lv_indev`；
     - `service_task`：Wi-Fi / SOCKS5 / RTC 周期维护；
     - `script_task`：JS 程序执行（SPM/SEC 已在单线程模型，需确认是否并入 ui_task）；
  3. 依次调用各驱动 `register(ops)`；
  4. 进入 Canto Mk.6 Core 启动（`eos_core_init` / `eos_core_run`）。
- 新建 `sdkconfig.defaults`：ESP32-S3、8MB Flash、`CONFIG_SPIRAM_MODE_QUAD` + `SPIRAM_SPEED_80M`、LVGL 相关、JerryScript 相关、关闭不必要的日志。
- 新建 `partitions.csv`：
  - `factory` app 分区（≥3MB 给 Core+Service+JS 引擎）；
  - `storage` / `data` 分区（FAT，存系统资源/配置，可选）；
  - `nvs`、`phy_init` 标准分区。
- 根 `CMakeLists.txt` 增加 `EOS_PLATFORM=ESP32` 分支：包含 `elenixos_lib`，按平台选源。

### 阶段 B · 真实硬件驱动（注册进 `devices` HAL）
- **display（GC9A01）**：在 `src/devices/display/` 新增 `eos_dev_display_gc9a01.c`，实现 `eos_dev_display_ops_t`（`set_brightness/power_on/power_off`）；并新建 LVGL 端口 `eos_hw_display.c`（`lv_display_create` + SPI `flush_cb` + PSRAM 帧缓冲）。模拟器继续用 `eos_virtual_display.c`。用宏分流：`#if EOS_PLATFORM == ESP32`。
- **touch（CST816）**：新增 `src/devices/touch/`（当前缺失），定义 `eos_dev_touch_ops_t { read, is_pressed }` 与 `eos_dev_touch_register/get_instance`；实现 I2C 驱动；建 LVGL `lv_indev` 端口（`read_cb`）。
- **storage（microSD）**：新增 `src/devices/storage/` 或扩展 `services/storage`，接 FATFS（SD SPI 或 SDMMC），实现 `mount/read/write/ls`，供 `EOS_SYS_DIR` 与 SD 卡访问。使得 `eos_app_install` 能从 SD 卡上的 `.eapk` 安装。
- **time / RTC（PCF8563 / BM8563）**：在 `src/devices/time/` 新增 I2C 驱动，实现 `eos_dev_time_ops_t`（已有 `get_datetime/set_datetime` 桩签名），boot 时 `settimeofday`。
- **统一宏开关**：`EOS_PLATFORM == Native` 走虚拟/桩，`== ESP32` 走真实驱动；HAL 注册在板级 `main.c` 完成。

### 阶段 C · LVGL / PSRAM / 圆屏统一
- 帧缓冲放 PSRAM：`lv_draw_buf_alloc(..., LV_DMA_... / MALLOC_CAP_SPIRAM)`（240×240×2 ≈ 115KB，双缓冲 ≈ 230KB，8MB PSRAM 充裕）。
- **圆屏裁剪下沉到 Core/System UI**：目前只有 Launcher 局部做了 `radius + clip_corner`（`eos_launcher.c::_make_round`）。抽出 `eos_round_clip(obj)` 工具，所有页面/Activity 统一调用，避免每处各做。
- 设计系统组件库：在 `src/ui/system/`（或扩 `src/ui/widgets/`）落地 `Label/Button/Icon/Card/List/Dialog/Slider/Page/Nav`，App 通过 SNI 调用，不再各写各的 UI。

### 阶段 D · 系统服务（Wi-Fi / SOCKS5 / 配置 / 资源）
- 网络服务（缺失，需新建 `src/services/network/`）：Wi-Fi Manager（Core 层）+ SOCKS5 Client（统一出口，非每 App 自实现）。配置存 `eos_service_config`（`proxy.enabled/host/port/username/password`，密码不明文）；Shell 查询只显示 `Auth: configured`。
- 资源管理器：图标（`icon.bin`）/ 字体 / 图片从 SD/Flash 加载，供 Launcher 与 App 使用（替代当前 Launcher 用字母色块占位）。

### 阶段 E · 插件装载链路收口（真机验证）
- 用 `.eapk`（+ manifest + `main.js`）在真机跑 Clock / Gallery 等，验证 SPM 装载 → SNI API → 权限隔离全链路。
- 把 Launcher 里 Clock 的临时 C activity 替换为真正从安装目录装载的插件。
- 收敛 Plugin API 命名面（建议沿用现有 `ui.*`≈SNI lv、`storage.*/rtc.*/network.*/proxy.*/input.*/settings.*/app.*`≈SNI eos，统一映射到 `sni_api_eos_*`）。

### 阶段 F · Shell（Core，不依赖 SD）
- 新建 `src/shell/`：USB Serial Shell，命令 `help/mem/heap/psram/flash/sd/rtc/display/touch/wifi/proxy/apps/app list/app start/app stop/log/reboot`（见 AGENTS.md §9-10）。即使 SD 缺失/损坏也必须可用。

### 阶段 G · 双构建维护
- 保证 Native 模拟器与 ESP32 真机都可在各自 CMake 下编译；HAL、显示、存储用 `EOS_PLATFORM` 宏分流。模拟器作为快速验证台，每次底层改动先在模拟器编译跑通。

---

## 4. 关键接口 / 数据结构（草案）

已有（直接复用）：
```c
// src/devices/display/eos_dev_display.h
typedef struct {
    void (*set_brightness)(uint8_t);
    void (*power_on)(void);
    void (*power_off)(void);
} eos_dev_display_ops_t;
eos_result_t eos_dev_display_register(const eos_dev_display_ops_t *ops);
eos_dev_display_t *eos_dev_display_get_instance(void);

// src/devices/time/eos_dev_time.h（同模式，ops 含 get_datetime/set_datetime）
```

需新增：
```c
// src/devices/touch/eos_dev_touch.h（当前缺失）
typedef struct {
    void (*read)(int32_t *x, int32_t *y);   // 或返回 lv_point_t
    bool (*is_pressed)(void);
} eos_dev_touch_ops_t;
eos_result_t eos_dev_touch_register(const eos_dev_touch_ops_t *ops);

// src/devices/storage/eos_dev_storage.h（当前缺失，或走 services/storage）
typedef struct {
    eos_result_t (*mount)(void);
    eos_result_t (*read)(const char*, uint8_t*, size_t, size_t*);
    eos_result_t (*write)(const char*, const uint8_t*, size_t);
    bool (*exists)(const char*);
} eos_dev_storage_ops_t;
```

脚本程序（已存在，`spm.h`）：
```c
typedef struct script_program {
    struct script_program *next, *prev;
    script_pkg_type_t type;          // APPLICATION / WATCHFACE
    script_program_state_t state;    // TERMINATED/STOPPING/ACTIVE/SUSPENDED
    sni_context_t *sni_ctx;
    script_pkg_t script;             // 含 id/name/permissions/min_api_level
    jerry_value_t realm;
    void (*cleanup_view)(void*);
    void *cleanup_user_data;
} script_program_t;
```

---

## 5. 风险与决策点

1. **插件是 `.eapk` 包，不是 AGENTS.md 设想的松散 SD 文件夹**。`/sdcard/apps/<App>/manifest.json` 那种布局在上游不存在；真实流程是 SD 上放 `.eapk` → `eos_app_install` 解包到 `EOS_SYS_DIR/app/apps/`。**建议保留 eapk（已成熟）**，SD 卡作为「安装源」而非「直接运行目录」。若你坚持松散文件夹，需要改 `eos_pkg_mgr` 与 SPM 装载逻辑（工作量大，不推荐）。
2. **SOCKS5 / Wi-Fi 必须真机 + 网络才能真验证**，模拟器用桩。
3. **PSRAM 帧缓冲**：240×240×2 双缓冲 ≈ 230KB，8MB PSRAM 充裕；注意 LVGL `lv_conf` 的 `LV_COLOR_DEPTH=16` 与 GC9A01 匹配。
4. **板级移植工作量最大**，是「底层大改」主体；建议优先做出「上电→GC9A01 出画面→CST816 触摸→Launcher 显示」的最小闭环，再往上叠服务。
5. **圆屏裁剪**要下沉一致，否则每个页面自己裁剪会出现 Launcher 已修过的「割裂/字体歪」类问题。

---

## 6. 建议开工顺序

**A（板级移植骨架）→ B.display + B.touch（先出画面+触摸）→ C（PSRAM/圆屏统一）→ A.剩余任务/分区 → D（网络）→ E（插件真机收口）→ F（Shell）→ G（双构建维护）**

每个阶段先在 Native 模拟器编译跑通，再在 ESP32 真机验证。

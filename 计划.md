Canto Mk.6 改造项目 — 工程级开发方案
基于实际源码分析的详细开发计划

一、项目理解
1.1 项目定位
维度	描述
产品形态	基于 XIAO ESP32-S3 + 圆形触摸屏的微型设备 OS
核心价值	Core 固件 + System Services + SD 卡插件 APP + 统一 UI
目标场景	掌上设备、智能家居面板、可穿戴式 UI 控制器
技术基线	ElenixOS（开源嵌入式脚本系统）
1.2 当前状态 — 已完成基线分析
根据 项目概况.md 报告：

模块	状态	质量评估
构建系统	CMake + ESP-IDF（推测）	✅ 可用
内核/框架	任务调度、事件、内存管理	✅ 完整
LVGL UI	完整集成，有主题/字体/控件	✅ 可用
脚本引擎	JerryScript (JS) 已集成	✅ 核心资产
APP 框架	eos_app 管理、package 管理器	✅ 可用
硬件抽象	devices/ 目录（显示/电池/传感器等）	⚠️ 需验证 XIAO 兼容性
Launcher	watchface/ 实现	⚠️ 需改造为圆屏 Grid
Shell	❌ 不存在	缺失
网络/Wi-Fi	❌ 未实现	缺失
SOCKS5 客户端	❌ 未实现	缺失
SD 卡插件加载	框架存在，但未按需实现	需改造
分区表/OTA	未确认	待查
1.3 已有模块详细清单
可直接复用（无需修改）:

text
src/script_engine/    → JerryScript 封装，JS 运行时
src/framework/app/    → eos_app 生命周期管理
src/framework/package/ → 包元数据解析
src/kernel/memory/    → eos_malloc/free (PSRAM感知)
src/services/config/  → 配置存储服务
src/services/storage/ → 文件存储服务
src/services/permission/ → 权限管理
src/ui/               → LVGL 主题/字体/控件基类
third_party/cJSON/    → JSON 解析
third_party/jerryscript/ → JS 引擎
需适配改造:

text
src/devices/display/  → 需改为 GC9A01 (240x240 圆屏)
src/devices/time/     → 需适配板载 RTC
src/framework/watchface/ → 改为 Launcher
src/apps/*            → 示例 APP 保留，改为圆屏 UI
需全新实现:

text
src/services/network/  → Wi-Fi 管理 + TCP/IP
src/services/proxy/    → SOCKS5 客户端
src/shell/             → USB/UART Shell 调试系统
src/framework/plugin/  → SD 卡插件加载器（强化）
src/ui/launcher/       → 圆屏 APP Grid
1.4 技术债务识别
债务项	严重性	说明
硬件平台未验证	🔴 高	当前 devices/ 非 XIAO 配置，需确认 GPIO/SPI/I2C
网络完全缺失	🔴 高	所有网络功能需从零实现
分区表未知	🟡 中	8MB Flash 如何分配尚不明确
PSRAM 使用策略不明	🟡 中	是否有 heap_caps 区分使用
插件加载未完整	🟡 中	框架存在，但未支持 SD 卡动态扫描
Shell 缺失	🟡 中	调试手段依赖 UI，存在风险
二、总体架构设计
2.1 系统分层架构（五层模型）
text
┌─────────────────────────────────────────────────────────────────────┐
│ L4  APP 层 (SD 卡插件)                                            │
│     Clock  Gallery  FileManager  Calculator  Timer  Games         │
│     └── 每个 APP = manifest.json + main.js + assets/              │
├─────────────────────────────────────────────────────────────────────┤
│ L3  系统服务层 (System Services)                                   │
│     ┌─────────┬─────────┬─────────┬─────────┬─────────┬────────┐ │
│     │ Network │  Proxy  │ Storage │ Plugin  │ Config  │  Time  │ │
│     │ Service │(SOCKS5) │ Service │ Manager │ Service │(RTC)   │ │
│     └─────────┴─────────┴─────────┴─────────┴─────────┴────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ L2  应用框架层 (Framework)                                         │
│     ┌──────────┬──────────┬──────────┬──────────┬──────────────┐ │
│     │ eos_app  │ Package  │ Activity │ Watchface│  Script      │ │
│     │ Manager  │ Manager  │  (UI)    │(Launcher)│  Engine(JS)  │ │
│     └──────────┴──────────┴──────────┴──────────┴──────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ L1  HAL/驱动层 (Devices + Port)                                    │
│     ┌─────────┬─────────┬─────────┬─────────┬─────────┬────────┐ │
│     │ Display │ Touch   │   SD    │  RTC   │ Battery │  GPIO  │ │
│     │ GC9A01  │(CST816) │  SPI    │(PCF85063)│ (ADC)  │ 抽象   │ │
│     └─────────┴─────────┴─────────┴─────────┴─────────┴────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ L0  RTOS + 硬件层 (ESP-IDF + ESP32-S3)                             │
│     FreeRTOS  │  SPI  │  I2C  │  SDMMC  │  USB  │  Wi-Fi/BLE    │
└─────────────────────────────────────────────────────────────────────┘
2.2 各层职责与接口
层级	职责	关键接口	生命周期
L4 APP	业务功能，UI 实现	EOS_APP 注册函数	按需加载/卸载
L3 Service	系统能力提供，跨 APP 共享	eos_service_* API	系统启动时初始化，常驻
L2 Framework	APP 管理、脚本运行、UI 导航	eos_app_*, eos_package_*	常驻
L1 HAL	硬件抽象，设备驱动	eos_device_*	启动时 init
L0 RTOS	任务调度、中断、硬件通信	ESP-IDF API	由 ESP-IDF 管理
2.3 数据流设计
APP 启动流程:

text
SD 卡扫描 → 读取 manifest.json → 注册到 Launcher 
→ 用户点击 → eos_app_start() → JS 引擎加载 main.js 
→ LVGL 创建 UI → APP 运行
网络请求流程 (经 SOCKS5):

text
APP JS 调用 fetch() → Network Service → SOCKS5 Client 
→ Wi-Fi → 外部代理服务器 → Internet
UI 刷新流程:

text
LVGL 定时器 (10ms) → 检测脏区域 → 渲染到 PSRAM Buffer 
→ DMA 发送 → GC9A01 刷新
三、详细开发路线
Phase 0：准备与基线确认（已完成）
目标： 确认当前代码与目标硬件匹配，消除不确定性

任务	状态	说明
扫描源码结构	✅ 已完成	见项目概况.md
确认构建系统	✅ 已完成	CMake + ESP-IDF
识别可复用模块	✅ 已完成	JerryScript / eos_app / cJSON
待完成：确认硬件配置	⚠️ 待执行	见下方 Phase 0.1
Phase 0.1：硬件配置确认（🔴 前置依赖）
目标： 确认当前代码中的 devices/ 配置与 XIAO ESP32-S3 + Round Display 匹配

任务清单：

#	任务	验证方式
0.1.1	查看 src/devices/display/ 配置，确认 LCD 驱动型号	搜索 GC9A01 / ST7789 / ILI9341
0.1.2	查看 src/devices/display/ SPI 引脚定义	确认 CS/DC/RST/BLK 对应 XIAO 引脚
0.1.3	确认触摸驱动（CST816?）是否存在	搜索 src/devices/input/
0.1.4	确认 SD 卡驱动是否存在	搜索 src/devices/storage/
0.1.5	确认 RTC 驱动（PCF85063?）是否存在	搜索 src/devices/time/
0.1.6	查看 partition table	查找 partitions.csv
完成标准： 输出《硬件兼容性报告》，明确哪些驱动需重写、哪些可复用

Phase 1：基础平台适配（Sprint 1-2，约2周）
目标： 让系统在 XIAO 硬件上正常启动、显示、触摸

任务 1.1：显示驱动适配（GC9A01 240x240 圆屏）
任务名称： display_gc9a01_port

目的： 替换/适配现有 display 驱动为 GC9A01，支持 240x240 圆形显示

输入： 现有 src/devices/display/ + Seeed GC9A01 示例代码

输出： src/devices/display/gc9a01/ 驱动

涉及文件：

新增：src/devices/display/gc9a01/gc9a01.c/h

修改：src/devices/display/display.c（统一接口）

修改：CMakeLists.txt

实现方式：

复用 Seeed 官方 GC9A01 初始化序列

SPI 通信使用 ESP-IDF SPI Master

实现 eos_display_flush() 对接 LVGL

测试方法： 显示纯色填充、画圆、LVGL 官方 demo

风险： SPI 引脚映射需确认（XIAO 圆屏使用 D10/D9/D8/D7）

任务 1.2：触摸驱动适配（CST816）
任务名称： touch_cst816_port

目的： 实现电容触摸读取，对接 LVGL 输入系统

输入： Seeed 触摸例程 + 现有 src/input/ 框架

输出： src/devices/input/cst816.c/h

实现方式：

I2C 通信读取触摸点

上报 LV_INDEV_TYPE_POINTER 事件

测试方法： LVGL 触摸校准 demo，画点跟随

风险： I2C 地址确认（0x15?）

任务 1.3：SD 卡驱动（SPI 模式）
任务名称： sd_spi_port

目的： 初始化 SD 卡，挂载 FATFS

输入： ESP-IDF SDMMC/SDSPI 示例

输出： src/devices/storage/sd_spi.c/h

实现方式：

使用 SPI 模式（XIAO 圆屏 SD 使用 SPI）

对接 src/services/storage/

测试方法： f_open 读写测试文件

风险： SD 卡供电/CS 引脚确认

任务 1.4：RTC 驱动（PCF85063）
任务名称： rtc_pcf85063_port

目的： 读取/设置 RTC 时间

输入： PCF85063 数据手册

输出： src/devices/time/pcf85063.c/h

实现方式： I2C 通信，实现 eos_time_get/set

测试方法： 读取时间与串口打印对比

Phase 1 完成标准：

□ 系统启动显示 Canto Mk.6 Logo
□ 触摸可操作
□ SD 卡可读写
□ RTC 可读取
Phase 2：Shell 调试系统（Sprint 3，约1周）
目标： 实现 USB/UART Shell，提供底层调试入口

任务 2.1：Shell 框架实现
任务名称： shell_framework

目的： 建立命令解析、注册、执行框架

涉及文件：

新增：src/shell/shell.c/h

新增：src/shell/commands/

实现方式：

使用 ESP-IDF console 组件

命令注册表（静态链表）

测试方法： 串口输入 help 显示命令列表

任务 2.2：基础命令实现
命令	功能	优先级
help	显示所有命令	P0
mem	显示内存使用（Internal + PSRAM）	P0
heap	显示堆信息	P0
ps	显示 FreeRTOS 任务列表	P0
sd	显示 SD 卡状态	P0
reboot	重启系统	P0
log level <level>	设置日志级别	P1
app list	列出已安装 APP	P0
app start <name>	启动 APP	P0
app stop <name>	停止 APP	P1
app info <name>	显示 APP 详情	P1
Phase 2 完成标准：

□ USB 串口输入 help 有响应
□ mem 正确显示 RAM/PSRAM 使用
□ app list 可列出 SD 卡 APP
Phase 3：网络 + SOCKS5 客户端（Sprint 4-5，约2周）
目标： 实现 Wi-Fi 管理和 SOCKS5 代理客户端

任务 3.1：Wi-Fi 服务
任务名称： network_wifi_service

目的： 实现 Wi-Fi 连接/断开/扫描/状态查询

涉及文件：

新增：src/services/network/wifi.c/h

新增：src/services/network/network_service.c/h

实现方式：

ESP-IDF Wi-Fi 事件循环

配置存储于 NVS（SSID/密码）

API 设计：

c
int network_connect(const char* ssid, const char* pass);
int network_disconnect(void);
int network_status(void);
int network_scan(wifi_ap_t* ap_list, int max);
任务 3.2：SOCKS5 客户端
任务名称： socks5_client

目的： 实现 RFC 1928 SOCKS5 协议，支持 CONNECT

涉及文件：

新增：src/services/proxy/socks5.c/h

新增：src/services/proxy/proxy_service.c/h

实现方式：

TCP Socket 连接代理服务器

实现握手、认证（无认证/用户名密码）

转发数据流

配置项：

text
proxy.enabled = false
proxy.host = ""
proxy.port = 1080
proxy.username = ""
proxy.password = ""  // 加密存储
测试方法： 连接测试代理服务器，验证 HTTP 请求通过

风险： ESP32-S3 Socket 资源限制，需管理连接池

任务 3.3：Network API 暴露给 JS
任务名称： js_network_binding

目的： 让 JavaScript APP 可以调用网络功能

涉及文件： src/script_engine/core/ 新增 binding

JS API 示例：

javascript
const response = await Canto Mk.6.fetch('https://api.example.com/data', {
  proxy: true  // 使用 SOCKS5
});
Phase 3 完成标准：

□ Shell wifi scan 可扫描 AP
□ Shell wifi connect <ssid> <pass> 可连接
□ Shell proxy status 显示代理状态
□ JS APP 可通过 ElenixOS.fetch() 发起网络请求（经代理）
Phase 4：Launcher + UI 系统（Sprint 6-7，约2周）
目标： 构建 240x240 圆屏 Launcher，统一的 UI 组件库

任务 4.1：Launcher 实现
任务名称： launcher_round

目的： 圆形 Grid 布局显示 APP 图标，点击启动

涉及文件：

新增：src/ui/launcher/launcher.c/h

新增：src/ui/launcher/app_grid.c/h

实现方式：

LVGL 自定义 Grid 布局（圆形裁剪）

从 Plugin Manager 获取 APP 列表

图标从 SD 卡加载

UI 设计：

text
┌─────────────┐
│  ⏰  📷  📁 │
│  🧮  ⏱️  🎮 │
│  ⚙️          │  ← 圆屏边缘裁剪
└─────────────┘
任务 4.2：System UI 组件库
任务名称： system_ui_components

目的： 统一的控件风格（按钮、卡片、列表、Dialog）

涉及文件： src/ui/widgets/ 扩展

实现方式：

基于 LVGL 样式系统

定义 Design Token（颜色、圆角、间距、字体）

输出： system_ui.h 提供快捷创建函数

任务 4.3：图标管理器
任务名称： icon_manager

目的： 从 SD 卡加载 APP 图标，缓存到 PSRAM

涉及文件： 新增 src/services/resource/icon_manager.c/h

实现方式：

读取 /sdcard/apps/<APP>/icon.png

转换为 LVGL 图像对象

LRU 缓存（限制 10 个图标）

性能要求： 图标加载不阻塞 UI（异步）

Phase 4 完成标准：

□ 启动后显示 Launcher Grid
□ 点击 APP 图标启动对应 JS 应用
□ 退出 APP 返回 Launcher
□ UI 动画流畅（≥30 FPS）
Phase 5：SD 卡插件系统强化（Sprint 8-9，约2周）
目标： 完整的插件生命周期管理、扫描、验证、启动/停止

任务 5.1：Plugin Manager 强化
任务名称： plugin_manager_enhance

目的： 实现 SD 卡 APP 扫描、注册、加载、卸载

涉及文件：

修改：src/framework/app/eos_app.c/h

新增：src/framework/plugin/plugin_scanner.c/h

功能清单：

扫描 /sdcard/apps/*/manifest.json

解析 manifest（name, version, entry, permissions, icon）

注册到 APP 注册表

启动时加载 main.js

退出时清理资源

manifest.json 格式:

json
{
  "id": "com.example.clock",
  "name": "Clock",
  "version": "1.0.0",
  "entry": "main.js",
  "icon": "icon.png",
  "permissions": ["storage.read", "time.read"],
  "author": "Elenix"
}
任务 5.2：JS 运行时环境增强
任务名称： js_runtime_enhance

目的： 提供标准 JS API 给 APP

API 设计：

javascript
// UI 操作
ElenixOS.ui.createPage(config)
ElenixOS.ui.createButton(config)
ElenixOS.ui.createLabel(config)

// 系统服务
ElenixOS.storage.readFile(path)
ElenixOS.storage.writeFile(path, data)
ElenixOS.time.getTime()
ElenixOS.time.setTime(timestamp)

// 网络
ElenixOS.fetch(url, options)

// 生命周期
ElenixOS.app.onPause(callback)
ElenixOS.app.onResume(callback)
ElenixOS.app.onDestroy(callback)
任务 5.3：APP 崩溃保护
任务名称： app_crash_protection

目的： APP 崩溃时不影响系统，自动返回 Launcher

实现方式：

JS 执行使用 jerry_port_set_uncaught_exception_callback

异常捕获后：清理 LVGL 对象 → 释放资源 → 返回 Launcher

测试方法： 注入 throw new Error('crash') 观察系统恢复

Phase 5 完成标准：

□ SD 卡 APP 自动扫描注册
□ Launcher 显示 APP 图标
□ APP 启动/退出正常
□ APP 崩溃不导致系统死机
□ app list Shell 命令显示已注册 APP
Phase 6：核心 APP 开发（Sprint 10-11，约2周）
目标： 开发 5+ 个示例 APP，验证插件系统

APP	复杂度	说明
Clock	中	数字/模拟时钟，RTC 同步
Gallery	中	SD 卡图片浏览（PNG/JPG）
File Manager	中	文件浏览/打开
Calculator	低	简单计算器
Timer	中	倒计时，通知
Settings	高	Wi-Fi/代理/显示设置
Phase 6 完成标准：

□ 5 个 APP 正常运行
□ 各 APP 图标显示正常
□ APP 间切换流畅
□ 内存占用稳定（无泄漏）
Phase 7：性能优化 + 量产准备（Sprint 12，约1周）
目标： 性能调优，达到量产标准

任务 7.1：LVGL 渲染性能优化
优化项	方法	目标
帧缓冲分配	使用 PSRAM heap_caps_malloc(MALLOC_CAP_SPIRAM)	释放内部 RAM
双缓冲	LVGL 双缓冲 + DMA	减少撕裂
局部刷新	lv_disp_set_driver_data() 标记脏区域	降低渲染负载
图片解码	预解码到 PSRAM	减少 SD 读取
任务 7.2：内存压力测试
测试内容：

连续启动/退出 APP 100 次，检查内存泄漏

同时运行多个 APP 切换

SD 卡高速读写 + UI 刷新同时进行

工具： Shell mem + ps 监控

任务 7.3：Flash 分区优化
分析当前 partition table

建议分区策略（8MB Flash）:

text
# Name,   Type, SubType, Offset,  Size
nvs,      data, nvs,     0x9000,  0x5000
otadata,  data, ota,     0xe000,  0x2000
app0,     app,  ota_0,   0x10000, 0x2F0000  # ~3MB
app1,     app,  ota_1,   0x300000,0x2F0000  # ~3MB
storage,  data, fat,     0x600000,0x200000  # 2MB
如果不需要 OTA: 移除 app1，扩大 storage 分区

任务 7.4：启动时间优化
减少不必要的初始化延迟

SD 卡检测异步进行

Launcher 优先显示，APP 图标懒加载

Phase 7 完成标准：

□ 持续运行 24 小时无内存泄漏
□ UI 帧率稳定 ≥ 30 FPS
□ 启动时间 < 3 秒（到 Launcher）
□ 所有 Shell 命令正常
四、每个任务拆解（详细模板）
以 任务 3.2 SOCKS5 客户端 为例：

任务详情：SOCKS5 客户端实现
字段	内容
任务名称	socks5_client
任务ID	T-3.2
所属Phase	Phase 3
预估工时	3 人天
输入
ESP-IDF Socket API 文档

RFC 1928 (SOCKS5 协议)

src/services/network/ 目录结构

输出
src/services/proxy/socks5.c — SOCKS5 协议实现

src/services/proxy/socks5.h — 公开 API

src/services/proxy/proxy_service.c — 统一代理服务

src/services/proxy/proxy_config.h — 配置管理

涉及模块
L3 系统服务层 (Network Service)

L0 TCP/IP Socket

预计修改文件
文件	操作	说明
src/services/proxy/socks5.c	新增	协议核心
src/services/proxy/socks5.h	新增	公开 API
src/services/proxy/proxy_service.c	新增	服务封装
src/services/CMakeLists.txt	修改	添加编译
src/config/eos_config_defaults.h	修改	添加代理配置项
实现方式
1. SOCKS5 协议状态机:

text
STATE_INIT → STATE_AUTH → STATE_REQUEST → STATE_CONNECTED → STATE_FORWARDING
2. 核心函数:

c
// 初始化 SOCKS5 客户端
socks5_handle_t* socks5_init(const socks5_config_t* config);

// 建立连接（阻塞，非阻塞可选）
int socks5_connect(socks5_handle_t* handle, const char* host, uint16_t port);

// 数据转发（由上层调用 select/poll）
int socks5_send(socks5_handle_t* handle, const uint8_t* data, size_t len);
int socks5_recv(socks5_handle_t* handle, uint8_t* buf, size_t len);

// 关闭
void socks5_close(socks5_handle_t* handle);
3. 认证支持:

无认证 (0x00)

用户名/密码 (0x02) — 使用配置中的 username/password

4. 错误处理:

连接超时：30s 后返回错误

代理拒绝：返回对应错误码

DNS 解析失败：返回 EAI_AGAIN

测试方法
单元测试（通过 Shell 命令）:

text
> proxy status
Enabled: yes
Server: 192.168.1.100:1080
Auth: password
Status: disconnected

> proxy test
Testing connection to proxy...
Connected successfully
Proxy version: SOCKS5
Authentication: OK
Test completed: PASS
集成测试（JS APP）:

javascript
const response = await ElenixOS.fetch('https://httpbin.org/ip', {
  proxy: true,
  timeout: 5000
});
// 验证返回的 IP 是代理服务器 IP
压力测试:

连续建立/关闭 1000 次连接

监控内存是否增长（heap_caps_get_free_size(MALLOC_CAP_INTERNAL)）

风险
风险	概率	影响	缓解措施
Socket 资源泄漏	中	高	使用 RAII 风格，goto cleanup 统一释放
代理 DNS 解析超时	中	中	设置 30s 超时，fallback 到直连
PSRAM 访问速度慢	低	中	Socket buffer 使用内部 RAM
用户名密码明文存储	中	高	使用 NVS 加密分区（如支持）
完成标准
□ socks5_connect() 可成功连接外部代理
□ 通过代理可访问 HTTP 网站
□ Shell proxy status 正确显示状态
□ 代理配置在重启后保留
□ 1000 次连接测试无内存泄漏
五、ESP32-S3 专项分析
5.1 资源预算分配
资源	总量	系统预留	APP 可用	说明
Internal RAM	512 KB	60% (307 KB)	40% (205 KB)	实时任务/中断/DMA/Socket 使用
PSRAM	8 MB	2 MB (缓存)	6 MB	LVGL 帧缓冲、图片、JS Heap
Flash	8 MB	Boot/Partition/NVS ~1MB	~3-4MB	Core + Services + JS Bytecode
5.2 内存分配策略
c
// 内部 RAM — 实时关键数据
void* dma_buffer = heap_caps_malloc(4096, MALLOC_CAP_DMA | MALLOC_CAP_INTERNAL);
void* task_stack = heap_caps_malloc(8192, MALLOC_CAP_INTERNAL);

// PSRAM — 大块数据
void* lvgl_buffer = heap_caps_malloc(240 * 240 * 2, MALLOC_CAP_SPIRAM);  // 115KB
void* image_cache = heap_caps_malloc(512 * 1024, MALLOC_CAP_SPIRAM);     // 512KB
5.3 FreeRTOS 任务规划
任务名	栈大小	优先级	周期	说明
IDLE	-	0	-	系统空闲
LVGL Task	4096	3	10ms	UI 刷新，最高优先级
WiFi Task	4096	2	事件驱动	Wi-Fi 事件处理
SD Worker	4096	1	消息队列	SD 卡读写，低优先级
Shell Task	4096	1	阻塞输入	USB 串口
Proxy Task	4096	2	事件驱动	SOCKS5 数据转发
APP JS Task	8192	2	消息队列	每个 APP 独立任务
约束： 总任务数 ≤ 10，上下文切换开销可控

5.4 中断与 DMA
中断	优先级	处理方式
SPI (LCD)	高	DMA 完成中断，ISR 尽量短
触摸 I2C	中	轮询方式（非中断）
SD 卡	中	使用 SPI 轮询，避免复杂 ISR
定时器 Tick	高	LVGL 心跳
关键原则： ISR 中绝对禁止打印日志、分配内存、调用 FreeRTOS 阻塞 API

5.5 LVGL 刷新性能设计
text
屏幕尺寸: 240 x 240 x RGB565 = 115,200 bytes

双缓冲策略:
  Buffer A: 240 x 240 x 2 = 115KB (PSRAM)
  Buffer B: 240 x 240 x 2 = 115KB (PSRAM)
  总计: 230KB PSRAM

刷新流程:
  1. LVGL 渲染到 Buffer A
  2. DMA 发送 Buffer A 到 LCD (SPI)
  3. LVGL 渲染到 Buffer B (并行)
  4. DMA 完成中断 → 切换显示
  5. 循环

目标帧率: 30 FPS (33.3ms/帧)
SPI 刷新时间: 115KB / 40MHz ≈ 23ms (理论)
实际可用渲染时间: 33.3 - 23 = 10ms
5.6 SD 卡 IO 策略
text
禁止:
  LVGL Task → SD Read → 等待 → 卡顿

正确:
  APP Request → SD Worker (消息队列) → 异步读取 
  → 存入 PSRAM Cache → LVGL Task 使用 Cache
5.7 OTA 可行性分析
方案	Flash 占用	优点	缺点
双备份 OTA	~6MB (app0+app1)	安全回滚	只剩 2MB 给 storage
压缩 OTA	~3MB (app0 + 压缩包)	节省 Flash	实现复杂，解压耗时
无 OTA (第一阶段)	~3MB app	最大 storage	升级需重新烧录
建议： 第一阶段不使用 OTA，将所有 Flash 空间用于 Core + Storage。后期再评估压缩 OTA 方案。

六、APP 插件化详细设计
6.1 APP 生命周期
text
                 ┌─────────┐
                 │ SCANNED │  ← 扫描到 manifest.json
                 └────┬────┘
                      ↓
                 ┌─────────┐
                 │REGISTERED│  ← 注册到 Launcher
                 └────┬────┘
                      ↓
                 ┌─────────┐
    用户点击      │ LOADING │  ← 加载 main.js，初始化
     ───────────→ └────┬────┘
                      ↓
                 ┌─────────┐
                 │ RUNNING │  ← 显示 UI，接收事件
                 └────┬────┘
                      ↓
          ┌──────────┴──────────┐
          ↓                     ↓
    ┌──────────┐          ┌──────────┐
    │  PAUSED  │          │ DESTROY  │  ← 用户退出 / 崩溃
    └────┬─────┘          └──────────┘
         ↓
    恢复运行
6.2 APP 目录结构
text
/sdcard/apps/
├── Clock/
│   ├── manifest.json
│   ├── main.js
│   ├── icon.png
│   └── assets/
│       ├── fonts/
│       └── images/
├── Gallery/
│   ├── manifest.json
│   ├── main.js
│   ├── icon.png
│   └── assets/
├── Settings/
│   ├── manifest.json
│   ├── main.js
│   ├── icon.png
│   └── assets/
└── ...
6.3 manifest.json 规范
json
{
  "id": "com.elenix.clock",
  "name": "Clock",
  "version": "1.0.0",
  "author": "Elenix Team",
  "description": "Digital and analog clock",
  "entry": "main.js",
  "icon": "icon.png",
  "permissions": [
    "time.read",
    "time.set",
    "storage.read"
  ],
  "required_system_version": "1.0.0",
  "features": {
    "supports_analog": true,
    "supports_digital": true,
    "supports_timezone": true
  },
  "size": 256  // KB
}
6.4 APP JS API 设计
生命周期函数:

javascript
// APP 入口
function onCreate() {
  // 创建 UI
  const page = ElenixOS.ui.createPage();
  const label = ElenixOS.ui.createLabel('Hello World');
  page.add(label);
  ElenixOS.ui.setCurrentPage(page);
}

function onPause() {
  // 暂停定时器、动画
}

function onResume() {
  // 恢复
}

function onDestroy() {
  // 清理资源
}
系统 API:

javascript
// UI
ElenixOS.ui.createPage(config)
ElenixOS.ui.createButton({ text, x, y, width, height, onClick })
ElenixOS.ui.createLabel({ text, x, y, color, font })
ElenixOS.ui.createImage({ path, x, y, width, height })

// Storage
ElenixOS.storage.readFile('/sdcard/apps/Clock/assets/config.json')
ElenixOS.storage.writeFile('/sdcard/data/clock.json', data)
ElenixOS.storage.listDirectory('/sdcard/apps')

// Time
ElenixOS.time.getTime()  // 返回 timestamp
ElenixOS.time.getDate()  // 返回 { year, month, day, hour, minute, second }
ElenixOS.time.setTime(timestamp)

// Network
ElenixOS.fetch(url, { method, headers, body, proxy: true })

// App
ElenixOS.app.getInfo()  // 返回 manifest 数据
ElenixOS.app.exit()     // 退出当前 APP
6.5 沙箱限制
限制项	实现方式
文件路径	禁止 ../ 跳出 /sdcard/apps/<APP>/
内存上限	APP 内存使用 > 512KB 时触发警告/终止
CPU 占用	JS 执行超时 100ms 后让出 (JerryScript 钩子)
系统 API	仅暴露白名单 API，不暴露底层硬件
6.6 图标管理
c
// 图标缓存策略
typedef struct {
    char app_id[32];
    lv_img_dsc_t* img_dsc;  // 解码后图像，存于 PSRAM
    uint32_t last_access;
} icon_cache_entry_t;

// LRU 缓存，最大 10 个图标
#define ICON_CACHE_MAX 10
6.7 升级方式
第一阶段： 整体固件升级（通过 USB 烧录）
后续： APP 独立升级（下载 .app 包 → 解压到 SD 卡对应目录）

七、开发管理
7.1 优先级排序（MoSCoW）
优先级	模块	说明
Must Have	显示驱动 (GC9A01)	无显示无法工作
Must Have	触摸驱动 (CST816)	无触摸无法交互
Must Have	SD 卡驱动	无 SD 无法加载 APP
Must Have	Launcher	系统入口
Must Have	Plugin Manager (基础)	扫描/注册/启动
Must Have	Shell (基础)	调试救援
Should Have	Wi-Fi 服务	网络功能基础
Should Have	SOCKS5 客户端	核心网络功能
Should Have	核心 APP (Clock/Settings)	系统可用性
Could Have	更多 APP (Gallery/Timer)	丰富功能
Could Have	OTA 升级	后期便利
Won't Have	BLE	非必须
7.2 MVP 版本定义
MVP (Minimum Viable Product) 范围:

模块	状态
✅ 系统启动	正常
✅ 显示 (GC9A01 240x240)	正常
✅ 触摸 (CST816)	正常
✅ SD 卡 (FATFS)	正常
✅ RTC (PCF85063)	正常
✅ Shell (基础命令)	可用
✅ Launcher (圆屏 Grid)	可用
✅ Plugin Manager (SD 扫描/加载)	可用
✅ 至少 1 个 APP (Clock)	可用
⬜ Wi-Fi	非必须
⬜ SOCKS5	非必须
⬜ 更多 APP	非必须
MVP 交付标准:

开机进入 Launcher

显示 APP 图标（至少 Clock）

点击启动 Clock APP

Clock 正确显示时间

退出返回 Launcher

Shell 可用

7.3 后续增强版本
版本	新增功能	说明
v1.1	Wi-Fi 管理	连接/断开/扫描
v1.2	SOCKS5 客户端	网络代理
v1.3	Settings APP	系统设置界面
v1.4	Gallery APP	图片浏览
v1.5	更多 APP	Timer/FileManager/Games
v2.0	OTA 升级	无线固件更新
7.4 每阶段验收标准
Phase	验收标准	验证方式
P0.1	硬件兼容性报告完成	文档审核
P1	显示/触摸/SD/RTC 正常工作	手动测试 + Shell 命令
P2	Shell 基础命令可用	串口交互测试
P3	Wi-Fi 连接 + SOCKS5 代理可用	网络连通性测试
P4	Launcher 显示 + APP 启动	UI 交互测试
P5	5 个 APP 从 SD 加载运行	完整流程测试
P6	5 个核心 APP 功能完整	功能测试
P7	性能达标 (30FPS, 无内存泄漏)	24h 压力测试
八、总结
8.1 关键决策
决策点	选择	理由
插件语言	JavaScript (JerryScript)	已有集成，生态成熟
APP 存储	SD 卡	8MB Flash 有限，SD 卡扩展
网络代理	SOCKS5 Client	需求明确，实现可控
调试方式	USB Serial Shell	无屏/UI 崩溃时的救援入口
OTA	第一阶段不支持	Flash 有限，降低复杂度

Phase	内容	
P0.1	硬件确认	
P1	基础平台适配	
P2	Shell 系统	
P3	网络 + SOCKS5	
P4	Launcher + UI	
P5	插件系统强化	
P6	核心 APP	
P7	性能优化	
总计		
8.3 风险总览
风险	等级	缓解措施
硬件驱动与 XIAO 不兼容	高	Phase 0.1 先行确认
8MB Flash 不足	中	移除 OTA，扩大 storage
动画性能不达标	中	PSRAM 双缓冲 + DMA
JS 引擎内存泄漏	中	压力测试 + 崩溃保护
SOCKS5 实现复杂	中	分阶段实现，先支持 CONNECT
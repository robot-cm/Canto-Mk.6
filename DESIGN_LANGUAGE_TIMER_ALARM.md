# 计时器 / 闹钟原型 · 设计语言（抽象原则，可迁移）

> 来源：`计时器.html`、`闹钟.html`（AI 同构生成的暗色原型）。
> 本文只提炼**可迁移的设计精神与原则**，不绑定具体字号/色值/间距——
> 落地时按目标平台（LVGL / Web / 其他）灵活应变，抓住"感觉"即可。

---

## 1. 设计气质（一句话）

**安静、未来感、高可读的暗色玻璃拟态**：半透明深色卡片悬浮在极淡的双色光晕上，
核心信息是巨大而纤细的数字，其余一切退到背景里。

---

## 2. 核心原则

### 2.1 暗底 + 极淡环境光，而非纯黑
背景不要死黑，用近黑底叠**两团极淡的对角径向光晕**（如一侧冷蓝、一侧冷紫）。
光晕强度以"几乎察觉不到、但让画面不沉闷"为准。这是整套设计的"签名氛围"。

### 2.2 玻璃卡片承载内容
内容收在**一块半透明深色卡片**里：磨砂模糊背后、细描边、柔和大投影、大圆角。
卡片四角可点几团同色系微弱光斑做点缀（不挡交互）。
→ 精神：用"材质"（玻璃）而非"边框/色块"来界定区域。

### 2.3 文字靠"明暗"分层，不靠"颜色"分层
信息层级用**白色的不同透明度**表达：主信息近白、次要信息渐隐、分隔符几乎隐形。
避免用多种彩色文字。彩色只留给"状态/语义强调"（见 2.5）。

### 2.4 数字是主角，且要"轻"
核心时间数字用**纤细字重 + 等宽数字**（对齐不跳动）。
附属单位（毫秒、秒、日期）明显小一号、更淡，紧挨主数字。
→ 精神：大、轻、稳，像仪表盘而非广告牌。

### 2.5 强调色按"语义"而非"装饰"使用
只保留少量强调色，且严格映射功能：
- 蓝系 = 主操作 / 进行中
- 红系 = 危险 / 停止 / 警示
- 暖色（橙）= 某一功能的专属识别色（如闹钟区别于计时器的暖调）

同一页面强调色不超过 2~3 种，平时按钮近乎中性灰白，激活才"亮"出语义色。

### 2.6 胶囊按钮 + 克制动效
按钮走**全圆角胶囊**形态；交互反馈统一用轻微位移（上浮/回弹缩放）+ 缓动曲线，
不闪不晃。状态变化时颜色从中性"点亮"到语义色。

### 2.7 动效服务于状态，不炫技
- 进度/状态用平滑过渡（线性或统一缓动）。
- 警示/响铃可用呼吸闪烁、图标轻摇摆等"提醒感"动效，但幅度小、节奏稳。
- 覆盖层（如响铃）用模糊背景 + 淡入，把注意力收拢到中心。

### 2.8 响应式：等比收敛，不破结构
小屏只做**字号、间距、圆角、按钮尺寸的等比收缩**，布局骨架不变。
移动端若太挤，允许设置区从横排转纵排，但仍是同一张卡片内。

---

## 3. 结构模板（抽象骨架，可套任何同类页）

```
居中容器
 └─ 半透明玻璃卡片
      ├─ 头部：左标签(疏字距大写)  +  右状态胶囊
      ├─ 主显示区：巨大纤细主数字(+ 更小更淡的附属单位)
      ├─ 辅助区：进度条 / 输入设置 / 列表（可选）
      ├─ 控制区：居中胶囊按钮组（主操作亮语义色，次操作中性）
      └─ 覆盖层：全屏模糊 + 居中提醒（可选，用于响铃/确认）
```

---

## 4. 行为气质（便于固件/逻辑重建）

- **状态显式化**：待命 / 进行中 / 已设定 / 响铃 等状态都要有可见反馈（状态胶囊变色、按钮切换语义色）。
- **键盘/手势友好**：主要操作应可一键触发（如空格开始暂停、Enter 确认、Esc 取消）。
- **计时精度看需求**：原型闹钟只精确到分钟、进度条为装饰循环——落地时按真实语义补精度与持久化。
- **提醒要有多通道兜底**：视觉（覆盖层+闪烁）+ 听觉/触觉（蜂鸣/振动），并设超时自动停止的保险。

---

## 5. 迁移时抓什么、放什么

**必抓（设计灵魂）**：
- 暗底双色极淡光晕的氛围
- 玻璃卡片而非色块分区
- 文字透明度分层、数字纤细等宽
- 语义化少量强调色
- 胶囊按钮 + 克制统一动效

**可放（灵活应变）**：
- 具体字号、圆角半径、模糊度数、光晕强度 → 按屏幕密度/平台默认值调
- 强调色具体色相 → 只要维持"蓝=主、红=危、暖=专属"的语义映射即可换色
- 动效时长/曲线 → 统一一种缓动即可，不必照搬数值
- 类名前缀、布局像素值 → 平台相关，自然重画

---

## 6. 已知取舍（落地注意）

1. 原型闹钟**精确到分钟**、进度条为**装饰循环**——固件实现需按真实语义补。
2. 音频依赖 Web Audio——固件侧换蜂鸣器/振动。
3. 状态**无持久化**——需接 RTC/存储（参考 `eos_service_time`）。
4. 纯单文件无构建——迁 LVGL 时把"原则"当设计稿，而非逐像素搬运。

---

## 7. 迁移硬约束：几乎零运行时开销（以 globaltime 为实例）

> 适用场景：把任意遵循本设计语言的页面迁移到受限嵌入式平台（如 1.28″ 圆屏、
> ESP32-S3、LVGL + eAPK JS 运行时）。**视觉可以升级，运行时成本必须接近零。**

### 7.1 零开销是硬要求，不是可选项
嵌入式小屏 + JS 运行时 + LVGL，**没有 GPU 兜底**。任何"每帧重算/全屏模糊/逐点投影"
的写法都会直接拖垮帧率甚至触发 task_wdt 致命重置（已有前车之鉴，见 7.3）。
迁移时默认假设：**页面静态，仅低频更新文本/样式，不进渲染热循环。**

### 7.2 玻璃拟态零成本落地法（不用真模糊）
- **禁止实时 `lv_blur` / `backdrop-filter` 逐帧计算**——这是性能炸弹。
- 用**静态半透明叠层 + 渐变光晕**模拟玻璃：卡片 `bgOpa < 255` + 同色系径向渐变点缀
 （`lv_obj_set_style_bg_grad` / 静态 `::before` 式装饰层），一次设置、永不重算。
- 文字透明度分层、胶囊按钮、语义色——全是 LVGL **一次性样式赋值**，零持续开销。

### 7.3 已知性能雷区（迁移时务必规避）
| 雷区 | 后果 | 处理 |
|---|---|---|
| 地球点阵 / 全量球面投影（globaltime 的 `geo2dots.js`） | DRAM 紧张 + task_wdt 重置（fatal code=10） | **已砍，迁移禁止恢复** |
| 实时模糊（`lv_blur`、逐帧 backdrop） | 每帧糊整屏，CPU 爆 | 改用静态半透明叠层模拟 |
| 把重活挂进高频率 tick | 卡顿/看门狗 | 文本更新降到 1Hz（如 globaltime 每 10 个 100ms tick 才刷新一次 HUD） |
| 逐帧 JS 动画计算 | 解释器开销累积 | 用 LVGL 内置 `lv_anim` 缓动（驱动层合成，几乎免费） |

### 7.4 globaltime 迁移基准（已验证轻量）
- 现状：6 按钮 + 2 label，HUD **每 1 秒**才重算一次文字，无动画/无模糊/无网络。
- 性能账：CPU 占用可忽略（<1% 空闲周期），DRAM 百字节级。
- 加玻璃拟态后仍是同一量级——**只要遵守 7.2/7.3，开销不变**。
- 时区为硬编码偏移（`hour + off - 8`），不计夏令时、不读系统时区语义，迁移时若
  `eos.time.getNow()` 语义变化需复核，但**不影响性能**。

### 7.5 迁移验收清单（性能维度）
- [ ] 无实时模糊/逐帧投影，玻璃感全靠静态样式
- [ ] 动态内容更新频率 ≤ 1Hz（或事件驱动，非每帧）
- [ ] 无把重计算挂进 tick/动画回调的写法
- [ ] 不动已砍的重负载模块（如地球点阵）
- [ ] 目标平台实测：空闲帧率无下降、无 WDT 重置

